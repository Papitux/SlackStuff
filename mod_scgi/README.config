Configuration
-------------

To enable, add

    LoadModule scgi_module /<some path>/mod_scgi.so

in your httpd.conf file.

To serve a set of URLs under one path with an SCGI server, use the SCGIMount directive:

    SCGIMount /dynamic 127.0.0.1:4000