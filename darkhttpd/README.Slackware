For convenience, the startup script 'rc.darhttpd' will be added to
your 'rc.local' and 'rc.local_shutdown' (if you have one).  But it
is commented by default to lower the number of daemons running  at
system startup. Feel free to uncomment them.